services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: margo
      POSTGRES_PASSWORD: margo_dev
      POSTGRES_DB: margo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
